const init = ''

const dialogsReducer = (state = init, action) => {
    switch (action.type) {
        case 1:
            return state;
        case 2:
            return state
        default:
            return state
    }
}
export default dialogsReducer